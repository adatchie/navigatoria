# Navigatoria HANDOFF - 2026-05-24

## 現在地

- 作業ルート: `C:\dol`
- アプリ本体: `C:\dol\app`
- ブランチ: `master`
- 現在のHEAD: `ff0cba0 Split portrait manager into output tabs`
- 直近のpush先: `origin/master`
- 現在の未コミット差分: `app/src/data/master/officers.json`
- 注意: `officers.json` の差分は、顔グラ管理ツール上で行われた航海士DBの手動調整らしい。勝手に戻さないこと。今回のタブ化コミットには含めていない。

## 重要な運用メモ

- `npm run lint` / `npm run build` は `C:\dol\app` で実行する。
- 手編集は `apply_patch` を使う。
- 既存のユーザー差分は戻さない。特に現時点では `app/src/data/master/officers.json` がdirty。
- 顔グラ管理ツールは `node tools\portrait-manager\server.mjs` で起動し、URLは `http://127.0.0.1:4178/`。
- アプリの開発サーバーは以前から `http://127.0.0.2:5173/navigatoria/` を主に使っている。`127.0.0.1:5173` は他ツールと競合しやすい。
- `tools/portrait-manager/data/*` はローカルDB/ジョブログで、Git管理外。生成履歴や顔グラDBの実体が入っている。

## 直近の実装状況

### 顔グラ・航海士まわり

- 航海士は生成式ではなく固定マスタ化済み。
- 固定マスタ: `app/src/data/master/officers.json`
- 84人構成: 7国籍 x 12人。
- 各国の専門分布は `航海3 / 交易3 / 砲術2 / 修理2 / 統率2`。
- 各航海士に以下を持たせている:
  - `id`
  - `name`
  - `firstName`
  - `familyName`
  - `nationality`
  - `gender`
  - `specialty`
  - `level`
  - `stats`
  - `hireCost`
  - `salary`
  - `portraitId`
  - `fallbackPortraitIds`
  - `homePortId`
  - `availablePortIds`
  - `minTavernLevel`
  - `minFame`
  - `displayOrder`
  - `description`
- ランタイムで能力値や顔グラをスコアリング生成しない。ゲーム中は固定マスタをローテーション表示するだけ。
- `app/src/game/officers/officerGenerator.ts` は、固定マスタを読み、港・酒場Lv・名声・雇用済みID/名前で候補を出す。
- `app/src/game/officers/officerPortraits.ts` は、`portraitId` から `app/src/data/master/officerPortraits.json` の `assetUrl` を引くだけの軽量処理。
- 顔画像は `app/public/generated/portraits/library/*.jpg` に入っている。

### 顔グラ管理ツール

- ツール: `tools/portrait-manager`
- 起動: `node tools\portrait-manager\server.mjs`
- URL: `http://127.0.0.1:4178/`
- 右ペインをタブ化済み:
  - `プロンプト送信`
  - `NPCプロフィール管理`
  - `顔グラDB`
- 左ペインの属性案作成・単体入力・一括入力は固定のまま。
- `NPCプロフィール管理` タブで `officers.json` を顔グラ付き一覧表示・編集できる。
- 編集対象:
  - 顔グラID
  - 表示名
  - 名/姓
  - 国籍
  - 性別
  - 専門
  - Lv
  - 能力値
  - 雇用費
  - 日給
  - ゆかり港
  - 出現港ID
  - 必要酒場Lv
  - 必要名声
  - 表示順
  - 背景/説明
  - 代替顔グラID
- 保存API:
  - `GET /api/officer-master`
  - `PATCH /api/officer-master/:id`
  - `GET /api/app-asset?path=...`
- `顔グラDB` タブは、従来の顔グラDBと生成画像フォルダをまとめて表示する。
- ブラウザ確認では、NPCタブで航海士84件、顔グラDBタブで顔グラ40件と生成画像40件が表示された。

## 現在の未コミット差分

`app/src/data/master/officers.json` に手動調整が残っている。

確認時点の概要:

- `git diff --numstat -- app/src/data/master/officers.json`: `21 insertions / 21 deletions`
- `updatedAt` が `2026-05-24T07:43:26.546Z` に変化。
- 例:
  - `officer:portugal:03` の専門が `gunnery` から `trade` へ変更。
  - `officer:portugal:03` の `portraitId` が `portrait_brief_moxjdesf_8btor` に変更。
  - `officer:portugal:03` の説明が「ポルトガルの元薬剤師...」系に変更。
  - `officer:portugal:10` が `ルイ・コエリョ` から `バルタザール・オリベイラ` に変更。
  - 複数の `portraitId` / `homePortId` / description が編集されている。

この差分はユーザーがツールで調整した可能性が高いので、次スレッドではまずユーザーに「この `officers.json` の手動調整をコミットするか」を確認するのがよい。

## 直近コミット

```text
ff0cba0 Split portrait manager into output tabs
7bc4f6d Add officer master editor to portrait manager
bb3e155 Use fixed tavern officer master data
da41940 Reserve hired officer portraits in taverns
740a0a9 Assign generated portraits to tavern officers
e45d47b Scale battle UI on compact screens
b693e94 Scale UI for mobile viewports
a32801b Restrict time flow to active sailing
```

## 検証済み

直近で確認したコマンド:

```powershell
cd C:\dol
node --check .\tools\portrait-manager\public\app.js
node --check .\tools\portrait-manager\server.mjs

cd C:\dol\app
npm run build
npm run lint
```

結果:

- `node --check`: 成功
- `npm run build`: 成功
- `npm run lint`: 成功
- 既知警告: `vendor-three` が500kB超過。従来からの警告。

ブラウザ確認:

- `http://127.0.0.1:4178/`
- 初期表示は `プロンプト送信`
- `NPCプロフィール管理` タブで `84 / 84件`
- `顔グラDB` タブで `40 / 40件`
- 生成画像フォルダは `40件`
- タブ切替後も左ペインの候補作成・入力枠は表示維持。

## 次スレッド開始時のおすすめ手順

```powershell
cd C:\dol
git status --short
git log -5 --oneline --decorate
git diff -- app/src/data/master/officers.json
```

その後、必要に応じて:

```powershell
node tools\portrait-manager\server.mjs
```

または既に起動していれば:

```powershell
Invoke-WebRequest -UseBasicParsing -Uri http://127.0.0.1:4178/api/officer-master
```

確認すべきこと:

1. `officers.json` の手動調整をコミットするか、さらに編集するか。
2. 顔グラ管理ツールのタブUIが実際の作業導線として十分か。
3. NPCプロフィール管理が航海士だけでよいか、今後は歴史NPC艦隊や酒場NPCも同じ画面に載せるか。
4. `officers.json` の編集時に `updatedAt` だけが変わるノイズをどう扱うか。今は保存APIが必ず更新する。
5. `app/src/data/master/officerPortraits.json` と `tools/portrait-manager/data/portrait-records.json` の同期方針。現状、ゲーム側に入っている顔グラライブラリと生成履歴DBは完全同一ではない。

## 注意すべきファイル

- `app/src/data/master/officers.json`
- `app/src/data/master/officerPortraits.json`
- `app/public/generated/portraits/library/`
- `app/src/game/officers/officerGenerator.ts`
- `app/src/game/officers/officerPortraits.ts`
- `app/src/components/screens/TownScreen.tsx`
- `tools/portrait-manager/server.mjs`
- `tools/portrait-manager/public/index.html`
- `tools/portrait-manager/public/app.js`
- `tools/portrait-manager/public/styles.css`
- `tools/portrait-manager/data/portrait-records.json` Git管理外

