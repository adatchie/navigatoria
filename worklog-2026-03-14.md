## 作業ログ 2026-03-14

1. **描画・デバッグ安定化**
   - `DebugPanel` の `Display` 各チェックは `toggleDebugFlag` から `setDebugFlag` に直して、Leva 再描画時に `showPortMarkers` などが意図せずオフになるのを防止。
   - 変更後のビルド (`npm run lint` / `npm run build`) も通過を確認済み。

2. **造船所と船管理**
   - `usePlayerStore` で `purchaseShip`/`setActiveShip` を整備し、船種データから実体を生成して資金・レベル/施設条件をチェック。
   - `TownScreen` の `Services` に `Shipyard` パネルを追加し、購入、所有船一覧、旗艦切替といった操作を確認できる構成へ。
   - 造船所購入条件は施設レベルやプレイヤーレベルと連動し、買えない場合はUIで説明。

3. **現在の状態**
   - 最新コミット `07f2851`（`Fix debug toggles and shipyard flow`）は origin/master に push 済み。
   - GitHub Actions では `lint`/`build`/`deploy-pages` が動作中、Pages も `https://adatchie.github.io/navigatoria/` で公開可能状態。

4. **次の手順**
   - 海上で `showPortMarkers` を On にした状態を確認。
   - 造船所で船購入・旗艦変更が正常か確認し、必要なら追跡バグのログも記録。
